# pisa-analysis

This repository includes data analysis code for processing successive images from PISA assay runs.

The v5 compartmentalization code processes individual PISA runs, outputting plots of several run metrics as well as aggregate data in a .json file. These output data files are parsed with the v4 concurrent plotting code to show concentration ladders and other data comparisons on shared axes.
